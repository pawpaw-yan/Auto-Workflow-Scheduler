# Python
__pycache__/
*.py[cod]

# 项目运行时产生的执行输出（由 common/render-summary.sh 渲染进 Job Summary）
output.log

# 每个脚本目录下的本地配置层（由 python/common/dotenv.py 在运行期读取）
# 只忽略 .env 本身；同目录的 .env.example 模板是要提交的
.env

# 本地保存的账号表（里面是 cookie / 令牌）。和 .env 同理 ——
# 只忽略这两个惯用的文件名，模板请另行命名（如 sites.example.txt）
sites.txt
sites.json
